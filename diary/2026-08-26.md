# 工作日志 · 2026-08-26

**项目**：QST「先锋号」鸿蒙智能小车（Harmony_Car）
**记录**：Zhuchuan Zhu（X-196）

---

## 今日完成

### 一、Linux 开发环境完善：SSH 远程连接 + 免密登录 + VMware Tools

**对应任务**：任务3 OpenHarmony 环境配置_Linux（收尾）

- **SSH 服务配置**：确认 Ubuntu 20.04 虚拟机（IP `192.168.124.129`，内核 5.4.0-52）SSH 服务状态，安装并启动 `openssh-server`（`systemctl is-active ssh` = `active`），22 端口连通性验证通过（`Test-NetConnection` → `TcpTestSucceeded: True`）。
- **VSCode Remote-SSH 连接**：配置 SSH 主机 `192.168.124.129`（用户 `harmony`），写入 `~/.ssh/config`，VSCode 连接 Linux、远程文件夹与终端使用。
- **SSH 免密登录**：生成 ed25519 密钥，公钥写入虚拟机 `~/.ssh/authorized_keys`，验证免密登录成功——以后 VSCode 和远程操作都不再需要密码。
- **VMware Tools 安装**：安装 `open-vm-tools` + `open-vm-tools-desktop`（版本 11.3.0.29534），服务 `vmtoolsd` / `vgauth` 均 active，重启虚拟机后生效——**Windows ↔ Ubuntu 剪贴板已同步**，远程使用体验完整。

### 二、U+ 平台阶段1任务清单梳理

- 通过课程项目接口（`projects/guides/tree`）拉取「QST先锋号鸿蒙智能小车系统」**阶段一完整任务清单**：共 **10 个任务、20 学时**（任务1 实训概述 / 任务2 OpenHarmony及Hi3861介绍 / 任务3 环境配置Linux / 任务4 环境配置Windows / 任务5 系统调试 / 任务6~9 红外、舵机、超声波、UART 驱动实验 / 任务10 阶段综合实验）。
- 对照现有进度：**任务3、任务4 的环境配置已完成**，阶段一整体进度 2/10。

### 三、任务4「OpenHarmony环境配置_Windows」补齐完成 ✅

对照 u+ 任务4 的 5 个步骤逐项诊断并补齐：

| # | 步骤 | 状态 |
|---|---|---|
| 1 | 安装 VSCode 及插件 | ✅ 已装（Remote-SSH 等） |
| 2 | 安装 RaiDrive | ✅ **新装**（RaiDrive Mount 2026.6.25） |
| 3 | 安装 CH340 驱动 | ✅ 已装（USB 转串口 COM9） |
| 4 | 安装下载烧录程序软件 | ✅ HiBurn（老师提供的课程资料 `tools/HiBurn`，**无需登录**，界面可正常打开） |
| 5 | 远程登录 Ubuntu | ✅ 免密 SSH 已配好 |

**任务4 全部 5/5 完成，可在 u+ 平台勾选。**

---

## 收获总结

1. 打通了从 Windows 到 Ubuntu 虚拟机的完整远程链路：**SSH 服务 → 免密密钥 → VSCode Remote-SSH → VMware Tools 剪贴板**。
2. 学会了借助 U+ 平台课程接口梳理任务清单；掌握了任务4 环境配置的完整要求并全部落地。
3. 认识了 HiBurn 烧录工具界面（COM 选择 → Connect → Select file → Add → 烧录），为任务5 实操做好准备。

## 下一步

- **任务5（OpenHarmony系统调试实验）**：连接 Hi3861 开发板（CH340 COM9），用 HiBurn 烧录第一个程序并仿真调试。
- 阶段一其余任务（6~10）按 u+ 平台步骤逐条推进。
